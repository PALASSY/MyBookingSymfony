{# navbar extrait dans bootswatch.com #}
<nav class="navbar navbar-expand-lg navbar-light
		bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path ('homepage')}}">Mybooking</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle
				navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('homepage')}} ">Accueil</a>
				</li>
			</ul>

			<ul class="navbar-nav ml-auto">
				{# Si l'utilisateur n'est pas null(existe) #}
				{% if app.user %}
					<li class="nav-item dropdown" role="group" aria-label="dropdown">
						<a href="#" class="nav-link dropdown-toggle" id="accountDropdownlink" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<img src="{{app.user.avatar}}" alt="Avatar de {{app.user.fullname}}" class="avatar avatar-small">
							{{app.user.fullname}}
						</a>
						{# Le lien #}
						<div class="dropdown-menu dropdown-menu-right headerdropdown" arial-labelledby="accountDropdownLink">
							<a href="{{path('ads_create')}}" class="dropdown-item">Créer une annonce</a>
							<div class="dropdown-divider"></div>
							<a href="{{path('account_user')}}" class="dropdown-item">Ma page de profil</a>
							<a href="{{path('account_profile')}}" class="dropdown-item">Modifier ma page de profil</a>
							<div class="dropdown-divider"></div>
							<a href="{{path('account_logout')}}" class="dropdown-item">Deconnexion</a>
						</div>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('account_login')}}">Connexion</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('account_register')}}">Inscription</a>
					</li>
				{% endif %}	
			</ul>

		</div>
	</div>
</nav>
